[pylint]
jobs=4
errors-only=yes
ignore=_librsync.so
msg-template='{path}:{line}: [{msg_id}({symbol}), {obj}] {msg}'

disable=all

enable=unused-argument,
       unused-wildcard-import,
       redefined-builtin,
       bad-indentation,
       mixed-indentation,
       unreachable
