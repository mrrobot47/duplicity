name: duplicity
version: $version
summary: Efficient, encrypted backup to local or remote hosts
description: |
  Duplicity backs directories by producing encrypted tar-format volumes and uploading
  them to a remote or local file server. Because duplicity uses librsync, the incremental
  archives are space efficient and only record the parts of files that have changed since
  the last backup. Because duplicity uses GnuPG to encrypt and/or sign these archives,
  they will be safe from spying and/or modification by the server.

grade: stable
confinement: classic
base: core20

apps:
    duplicity:
        command: bin/duplicity

parts:
    duplicity:
        plugin: python
        python-version: python3
        build-packages:
            - build-essential
            - ieee-data
            - intltool
            - librsync-dev
            - python3-dev
        stage-packages:
            - gnupg
            - lftp
            - libatm1
            - librsync2
            - ncftp
            - openssh-client
            - python3-gi
            - rsync
            - rclone
        python-packages:
            - azure-storage-blob            ; python_version == '3.6'
            - b2sdk                         ; python_version == '3.6'
            - boto                          ; python_version == '3.6'
            - boto3                         ; python_version == '3.6'
            - boxsdk[jwt]                   ; python_version == '3.6'
            - dropbox                       ; python_version == '3.6'
            - fasteners                     ; python_version == '3.6'
            - future                        ; python_version == '3.6'
            - gdata-python3                 ; python_version == '3.6'
            - google-api-python-client      ; python_version == '3.6'
            - google-auth-oauthlib          ; python_version == '3.6'
            - httplib2                      ; python_version == '3.6'
            - jottalib                      ; python_version == '3.6'
            - keyring                       ; python_version == '3.6'
            - mediafire                     ; python_version == '3.6'
            - megatools                     ; python_version == '3.6'
            - paramiko                      ; python_version == '3.6'
            - pexpect                       ; python_version == '3.6'
            - psutil                        ; python_version == '3.6'
            - pydrive                       ; python_version == '3.6'
            - pyrax                         ; python_version == '3.6'
            - python-swiftclient            ; python_version == '3.6'
            - requests                      ; python_version == '3.6'
            - requests-oauthlib             ; python_version == '3.6'
            - setuptools                    ; python_version == '3.6'
            - setuptools-scm                ; python_version == '3.6'
            - sx                            ; python_version == '3.6'
            - tahoe-lafs                    ; python_version == '3.6'
            - urllib3                       ; python_version == '3.6'
        source: .
